[package]
name = "rusty_emu"
version = "0.1.0"
edition = "2021"

[dependencies]
tempfile = "3.3.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
# Property-based testing
proptest = "1.0"
